# Tasks

1. Schema + writers for exchange_status, options_mark, options_index
2. System status poller; Options REST pollers + instrument enrich + WS URL/case fix
3. Validator disabled-product handling; inventory update
4. Smoke config (all products, BROAD tiers); multi-product run + validate
5. settings.yaml kline intervals; docs/CHANGELOG/STATUS/index/tracker
