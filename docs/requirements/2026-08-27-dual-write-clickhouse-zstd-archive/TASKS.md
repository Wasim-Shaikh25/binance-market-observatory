# Tasks

1. Config dataclasses + settings.yaml / smoke_settings sinks
2. RawArchiveWriter (zstd NDJSON)
3. ClickHouseSink (schema + batch insert)
4. Wire DBWriter + main; dependency zstandard
5. Tests + docs
