# Design

Config-only. No connector code changes. Options stays `symbol_universe: list`
with a few contracts so REST weight stays bounded; instruments still come from
`exchangeInfo` for those symbols.
