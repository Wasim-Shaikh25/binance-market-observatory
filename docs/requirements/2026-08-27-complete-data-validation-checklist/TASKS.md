# Tasks

1. Requirement docs + index entry
2. Inventory + CheckResult status model
3. DB/schema/integrity/timestamp/duplicate/coverage checks
4. Sample raw↔normalized + optional trade/candle & depth/bookTicker reconciliations
5. Report renderer + CLI `python -m src.validate`
6. Unit tests for status helpers / inventory
7. Run against latest smoke DB → write `BINANCE_DATA_CAPTURE_REPORT.md`
8. CHANGELOG / STATUS update
