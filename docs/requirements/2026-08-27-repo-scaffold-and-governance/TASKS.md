# Tasks: Repo Scaffold & Governance

1. Write `docs/THESIS.md` — full thesis covering purpose, scope table, core principles,
   data model decisions, storage architecture, capability registry, health/audit
   philosophy, build order, and explicit non-goals.
2. Write `docs/SCOPE.md` — enforced in/modify/never-add checklist derived from the thesis.
3. Write `docs/ARCHITECTURE.md` — component diagram, data flow, core table shapes,
   capability registry summary, tech-stack default, target repo layout.
4. Write `docs/requirements/README.md` — workflow how-to and folder index.
5. Create this folder (`2026-08-27-repo-scaffold-and-governance`) with
   REQUIREMENTS/DESIGN/TASKS/TRACKER documenting this exact change.
6. Create `2026-08-27-phase1-spot-collector` with REQUIREMENTS/DESIGN/TASKS/TRACKER for
   the next unit of work, tracker unchecked.
7. Write root `AGENTS.md` — scope-guard rule, modularity rule, requirement workflow
   rule, tracker discipline, changelog/status discipline, docs-sync rule, and the
   end-of-task self code-review prompt.
8. Write root `CHANGELOG.md`, seeded with an entry for this change.
9. Write root `STATUS.md`, reflecting the state after this change.
10. Rewrite root `README.md` to link `AGENTS.md`, `docs/THESIS.md`, `docs/SCOPE.md`,
    `docs/ARCHITECTURE.md`, `docs/requirements/`, `CHANGELOG.md`, and `STATUS.md`.
11. Update this folder's `TRACKER.md` as each task above completes.
12. Update `docs/requirements/README.md`'s index table to mark this folder Done.
