# Tasks

1. Fix COIN-M `pair=` query param + pair derivation helper
2. Replace COIN-M `takerlongshortRatio` with `takerBuySellVol` row split
3. Fix depth buffer-during-resync storm
4. Add unit tests for the above
5. Live smoke verify against Binance; confirm table coverage + low resync counts
6. Update CHANGELOG / STATUS / requirements index
