# Tasks

1. Schema: `raw_events.source_type` + migration; Envelope field
2. Options parsers + REST depth/OI polls; wire into `rest_market_loop`
3. Storage write path for source_type; tests
4. Inventory + validator updates
5. Live Options smoke (short) + docs/CHANGELOG/STATUS/ARCHITECTURE/index/tracker
