# research/

Downstream scripts and reports that **consume** observatory databases.

| Path | Purpose |
|---|---|
| `script/` | Analysis / exploration scripts |
| `report/` | Outputs those scripts generate |

See `AGENTS.md` rule 8. Do not put trading strategies, signals, or ML training here
(`docs/SCOPE.md` 🔴 list) — that belongs in a separate project.
